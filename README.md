# git_tuto
learning git commands
<br>

# cloning repo on local machine
git clone
ls

# to see hidden files 
ls-a in bash 
ls-Force in powershell

# to see status
git status

# add new or changed files in your working dir to the git staging area
1. git add filename
2. git add. ->add all files

# commit is the record of change
git commit -m "some message like update "

# push command
git push origin main -> here origin is the name of the copy exist on github and main is the branch

# init command
1. git init



